import {fork} from 'redux-saga/effects';
// import {watchFiles, watchResource, watchProvenance} from './watcher';

export default function * startForman() {
  // yield fork(watchFiles);
  // yield fork(watchResource);
  // yield fork(watchProvenance);
}